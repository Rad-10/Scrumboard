import { FunctionComponent, Ref } from 'react';
import { BaseProps, ForwardProps } from '../../types';
import { LatLng } from './types';
export interface LocationDisplayProps extends BaseProps {
    /** Location address or coordinates */
    value?: string | LatLng;
    /** The text to be displayed. */
    displayText?: string;
    /**
     * Controls styling of the field.
     * @default "text-map"
     */
    variant?: 'text-map' | 'text' | 'map';
    /**
     * Height of the map container.
     * @default '25rem'
     */
    height?: string;
    /**
     * How much the map should zoom.
     * @default 13
     */
    zoomLevel?: number;
    /** Ref for the wrapping element. */
    ref?: Ref<HTMLAnchorElement | HTMLSpanElement>;
}
declare const LocationDisplay: FunctionComponent<LocationDisplayProps & ForwardProps>;
export default LocationDisplay;
//# sourceMappingURL=LocationDisplay.d.ts.map