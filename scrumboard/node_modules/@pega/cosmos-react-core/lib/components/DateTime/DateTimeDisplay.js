import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { useConfiguration, useI18n } from '../../hooks';
import { parseToDate } from './Input/utils';
import { isValidDate } from './Picker/utils';
import { formatDateTime } from './utils';
const DateTimeDisplay = forwardRef(({ value, variant, format = 'short', clockFormat, ...restProps }, ref) => {
    const { locale } = useConfiguration();
    const t = useI18n();
    return (_jsx("span", { ref: ref, ...restProps, children: (value || value === 0) && isValidDate(parseToDate(value)) ? (formatDateTime(parseToDate(value), {
            t,
            locale,
            format,
            variant,
            clockFormat,
            timeZone: 'UTC'
        })) : (_jsx("span", { "aria-hidden": 'true', children: "\u2013\u2013" })) }));
});
export default DateTimeDisplay;
//# sourceMappingURL=DateTimeDisplay.js.map